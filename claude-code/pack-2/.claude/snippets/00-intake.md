# Intake prompt

Please provide:
- `model_name`
- Source path (default: `transformers/src/transformers/models/{model_name}/`)
- Target path (default: `mindone/mindone/transformers/models/{model_name}/`)
- Any `processing_*.py`, `image_processing_*.py`, or `video_processing_*.py` files
- Reference HF test file (if known)
